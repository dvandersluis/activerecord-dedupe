git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

source 'https://rubygems.org'

# Specify your gem's dependencies in activerecord-dedupe.gemspec
gemspec
gem 'rubocop_defaults', github: 'dvandersluis/rubocop_defaults', branch: '0.84'
